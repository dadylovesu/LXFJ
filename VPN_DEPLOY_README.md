# 🔓 VPN代理部署指南 - 无需用户开启VPN

## 📋 方案概述

通过VPN代理部署，让**你开启VPN**，其他用户**无需VPN**即可正常使用AI功能。

**访问地址**：`http://192.168.10.123:3000`

## 🚀 快速启动

### 方法一：双击启动（推荐）
1. 双击 `start.bat` 文件
2. 等待自动安装依赖并启动服务

### 方法二：PowerShell启动
```powershell
.\start.ps1
```

### 方法三：手动启动
```bash
# 终端1：启动前端
npm run dev

# 终端2：启动后端
cd server
npm start
```

## ⚙️ 配置说明

### 1. 代理配置 (server/env.config)

根据你的VPN/代理软件修改端口：

```env
# Clash 默认端口 (推荐)
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890

# V2Ray 默认端口
HTTP_PROXY=http://127.0.0.1:10809
HTTPS_PROXY=http://127.0.0.1:10809

# 系统VPN（全局模式）
# 如果开启全局VPN，注释掉上面的两行
```

### 2. 端口配置

- **前端**: `http://192.168.10.123:3000` (已配置)
- **后端**: `http://192.168.10.123:3001` (已配置)

## 🔧 工作原理

```
用户访问 (无需VPN)
    ↓
http://192.168.10.123:3000 (前端)
    ↓
http://192.168.10.123:3001 (后端代理)
    ↓
Google Gemini API (通过你的VPN访问)
```

## 📋 部署步骤

### 1. 开启你的VPN
确保VPN正常工作，能访问Google服务。

### 2. 启动服务
运行 `start.bat` 或 `.\start.ps1`

### 3. 验证部署
- 访问 `http://192.168.10.123:3001/health` 检查后端状态
- 访问 `http://192.168.10.123:3000` 验证前端功能

### 4. 分享给其他用户
其他用户直接访问 `http://192.168.10.123:3000` 即可使用，无需开启VPN。

## 🔍 故障排除

### 后端启动失败
```bash
cd server
npm install
npm start
```

### 前端启动失败
```bash
npm install
npm run dev
```

### API调用失败
1. 检查VPN是否正常工作
2. 验证代理端口配置是否正确
3. 查看后端日志中的代理配置信息

### 网络连接问题
- 确保防火墙允许3000和3001端口
- 检查本地网络设置
- 验证IP地址 `192.168.10.123` 是否正确

## 📊 状态监控

访问 `http://192.168.10.123:3001/health` 查看系统状态：

```json
{
  "status": "ok",
  "gemini_api": "已配置",
  "runninghub_api": "已配置",
  "timestamp": "2024-01-14T..."
}
```

## 🎯 使用建议

1. **网络稳定**: 确保VPN连接稳定
2. **端口开放**: 确认防火墙允许相应端口访问
3. **IP确认**: 验证本地IP地址正确
4. **定期检查**: 定期检查服务运行状态

## 📞 技术支持

如遇到问题，请检查：
1. VPN连接状态
2. 代理端口配置
3. 服务启动日志
4. 网络连接状态